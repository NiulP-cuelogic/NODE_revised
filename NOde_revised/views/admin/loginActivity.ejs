<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/loginActivity.css">
    <title>Document</title>
</head>
<body>

    <!-- <div class="limiter">
        <div class="container-table100">
            <div class="wrap-table100">
                <div class="table">
                    <div class="row header">
                        <div class="cell">User email</div>
                        <div class="cell">Date and time</div>
                        <div class="cell">Date now</div>

                    </div>
                    <div class="row">
                            <% for(var i=0; i<user.length;i++) { %>
                                    <div class="cell"> 
                                        
                                        <div class="cell"> <%=user[i].user_email%></div>
                                        <div class="cell"> <%=user[i].date%></div>
                                        <div class="cell"> <%=user[i].loginDate%></div>
                                        
                                        
                                    </div>
                                  <% } %>
                    </div>
                    
                </div>
            </div>
        </div>
    </div> -->
    <div class="container">
        <table class="users">
           
                <tr>
                    <th>User-email</th>
                    <th>Recent logins </th>
                   
                </tr>
            
            <tbody>
                <% for(var i=0; i<user.length;i++) { %>
                    <tr id="row_<%=i%>"> 
                    
                        <td> <p> <%=user[i].user_email%></p></td>
                        <td>  <%=user[i].loginDate%></td>
                        
                        
                    </tr>
                  <% } %>
            </tbody>
        </table>    
</body>
</html>